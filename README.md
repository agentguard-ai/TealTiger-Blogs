# TealTiger Blog (GitHub Pages)

This repo hosts the TealTiger blog on GitHub Pages.

## Local preview (optional)
If you have Ruby/Jekyll installed:

```bash
bundle exec jekyll serve
```

## Publishing
Merge to `main` → GitHub Pages publishes automatically.

## Cross-linking
Use `{{ site.docs_base }}` for stable links back to documentation.
